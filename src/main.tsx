import React from 'react'
import ReactDOM from 'react-dom/client'
import ArtistStatusTracker from './artist-status-tracker'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ArtistStatusTracker />
  </React.StrictMode>
)
